# 자료구조 수행준비
## 1.문제 주제 : 좀비 시뮬레이터

## 2. 해당 주제를 선택한 이유 :

# 파기 - 실생활과 관련되지 않은 문제와 상황임 상황자체는 좋았음.

## 1. 문제 주제 : 학교 비상 연락망 공지 전달 프로그램

## 2. 주제 선정 이유 : 
학교에선 자주 급하게 전달해야하는 사항이 있는데 이를 더 신속하게 전달 할 수 있는 프로그램이 필요하다고 생각함.
이때 선생님이 모든 학생에게 전달하는거 보다 반장, 학생 대표를 통해 차례대로 전달하는 방법 사용.

이러한 연락 관계는 사람을 정점으로, 연락 가능한 관계를 간선으로 표현할 수 있으므로 그래프 구조와 잘 맞는다.
또한 공지가 처음 전달된 사람부터 가까운 관계의 학생들에게 순서대로 퍼지므로, BFS 알고리즘을 활용하기에 적절하다고 판단함.

## 3. 문제상황 : 
어느 학급에 비상 연락망이 있다
각 학생은 자신과 연결된 학생에게 공지를 전달 할 수 있다.
* 처음 공지를 받은 학생이 시작 정점이 된다.
* 해당 학생과 직접 연결된 학생들이 먼저 공지를 받는다.
* 그다음, 공지를 받은 학생들과 연결된 다른 학생들에게 차례대로 공지가 전달된다.
* 이미 공지를 받은 학생에게는 다시 전달하지 않는다.

## 4. 그래프 분석
- 정점 : 공지를 전달받을 수 있는 학생을 의미한다.
- 간선 : 두 학생 사이에 연락이 가능한 관계를 의미한다.
- 방형 여부 : 무방향 그래프 _ 한 학생이 다른 학생에게 연락 할 수 있다면 반대도 가능하다고 가정.
- 가중치 여부 : X
- 이 문제에서는 시간이나 거리보단 공지가 전달되는지가 중요하다고 생각함.

# 5. 문제
비상 연락망에서 학생들의 연락 관계와 처음 공지를 받은 학생이 주어졌을 때, BFS를 활용하여 공지가 전달되는 순서를 출력하는 프로그램을 작성

# 6. 입력 조건
학생수 N과 연락 관계 수 M이 입력된다
N은 6이상 M은 7이상 이여야한다.

두번째 줄 부터 N개의 줄에 학생 입력

그다음 M개의 줄에 서로 연락 가능한 두 학생의 이름이 입력된다.

마지막 줄에 처음 공지를 받은 학생의 이름이 입력된다.

# 7. 출력 조건

공지 전달 순서를 출력한다.
또한 총 몇 명에게 공지가 전달되었는지 출력한다.

# 8. 문제 해결 조건

* 정점은 6개 이상이어야 한다.
* 간선은 7개 이상이어야 한다.
* BFS 알고리즘을 사용한다.
* BFS는 큐 구조를 활용하여 구현한다.
* 이미 방문한 학생은 다시 큐에 넣지 않는다.
* 시작 학생으로부터 연결된 학생들에게만 공지가 전달된다.


## 학교 비상 연락망 공지 전달 프로그램
## BFS 알고리즘 사용
# #큐를 직접 구현

# BFS 함수
def bfs(graph, start):

    # 방문한 학생 저장
    visited = []

    # 큐 생성
    queue = []

    # 시작 학생 큐에 삽입
    queue.append(start)

    # 큐가 빌 때까지 반복
    while len(queue) > 0:

        # 큐의 맨 앞 데이터 꺼내기
        current = queue.pop(0)

        # 아직 방문하지 않았다면
        if current not in visited:

            # 방문 처리
            visited.append(current)

            # 연결된 학생 확인
            for next_student in graph[current]:

                # 아직 방문하지 않았고
                # 큐에도 없는 경우
                if next_student not in visited and next_student not in queue:

                    # 큐 뒤에 삽입
                    queue.append(next_student)

    return visited


# 학생 수 입력
n = int(input("학생 수 입력: "))

# 연락 관계 수 입력
m = int(input("연락 관계 수 입력: "))

# 그래프 생성
graph = {}

# 학생 이름 입력
for i in range(n):

    name = input("학생 이름 입력: ")

    graph[name] = []


# 연락 관계 입력
for i in range(m):

    a, b = input("연락 가능한 두 학생 입력: ").split()

    # 무방향 그래프
    graph[a].append(b)
    graph[b].append(a)


# 시작 학생 입력
start = input("처음 공지를 받은 학생 입력: ")

# BFS 실행
result = bfs(graph, start)

# 결과 출력
print("\n공지 전달 순서")

for student in result:
    print(student, end=" ")

print("\n총 전달 인원:", len(result), "명")

# 11. 구현 결과 분석

이 프로그램은 학생들의 연락 관계를 그래프로 저장한 뒤, 시작 학생을 큐에 넣고 BFS 탐색을 수행한다.
큐에서 학생을 하나씩 꺼내며 아직 방문하지 않은 학생이면 방문 처리하고, 그 학생과 연결된 학생들을 큐에 추가한다.

이를 통해 공지가 실제로 어떤 순서로 전달되는지 확인할 수 있다.