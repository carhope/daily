# 컴퓨터 구조 수행평가
## 💡 요약
CPU가 명령어를 처리하는 주소 형식(3, 2, 1, 0-주소)의 메커니즘을 이해하고, 간접 주소 지정 방식에서 레지스터 간 데이터 이동(마이크로 연산) 흐름을 마스터함.
## 어셈블리어 & 주소 지정 방식 (Addressing Mode)
CPU가 명령어를 실행할 때 오퍼랜드(데이터 또는 주소)를 어떻게 찾아가는지 결정하는 규칙.
주소표, opcode, 주소 지정방식 표가 주어진 프로그램을 보고 어셈블리어로 FETCH, DECODE, EXCUTE를 할 수 있음.


---
# 영어 단어, 시험 지문 1회 작성
## 문제 1. 배열 초기화

다음 코드의 출력 결과를 쓰시오.

```c
#include <stdio.h>

int main() {
    int arr[5] = {1, 2};

    for (int i = 0; i < 5; i++) {
        printf("%d ", arr[i]);
    }

    return 0;
}
```
12000 정답
## 문제 2. 배열과 주소

`int`가 4바이트이고 `arr[0]`의 주소가 1000일 때, 다음 배열의 각 요소 주소를 쓰시오.
1004
1008
1012
..
정답
## 문제 3. 문자열 크기

다음 배열의 크기는 몇 바이트인가?

```c
char str[] = "ABC";
```

--- 4바이트 X

## 문제 4. 포인터 기본

다음 코드의 출력 결과를 쓰시오.

```c
#include <stdio.h>

int main() {
    int a = 10;
    int *p = &a;

    *p = 30;

    printf("%d\n", a);

    return 0;
}
```
30 정답

## 문제 5. 배열과 포인터

다음 코드의 출력 결과를 쓰시오.

```c
#include <stdio.h>

int main() {
    int arr[3] = {10, 20, 30};
    int *p = arr;

    printf("%d\n", *(p + 1));

    return 0;
}
```
20 정답

---

## 문제 6. 2차원 배열 저장 순서

다음 배열은 메모리에 어떤 순서로 저장되는가?

```c
int arr[2][3] = {
    {1, 2, 3},
    {4, 5, 6}
};
```
0,0 0,1 0,2 1,0 1,1 1,2 정답
---

##

## 문제 7. 포인터 배열과 배열 포인터 구분

다음 중 포인터 배열은 무엇인가?

```c
A. int *arr[5];
B. int (*arr)[5];
```
A 틀림 B인줄
---

## 문제 8. 잘못된 코드 찾기

다음 코드에서 잘못된 부분을 찾고 이유를 설명하시오.

```c
int arr[3] = {1, 2, 3};
arr++;
```
배열을 늘리는건 상식적으로 안되지않나? - 배열이름은 주소를 나타낼 수 있지만 포인터가 아니기 떄문에 안됌
## 문제 9. 문자열 수정

다음 코드가 위험한 이유를 설명하시오.

```c
char *str = "hello";
str[0] = 'H';
```
잘못된 접근(읽기 전용)
---

## 문제 10. 동적 할당

다음 코드에서 빠진 부분을 채우시오.

```c
#include <stdio.h>
#include <stdlib.h>

int main() {
    int n = 5;
    int *arr = malloc(n * sizeof(int));

    if (arr == NULL) {
        return 1;
    }

    // 메모리 해제
    free(arr); 정답

    return 0;
}
```

# 탑싯으로 마역량 챙기기

# 여름방학은 하고싶은거 하기

# AI 활용 해서 학습하기

take risk: 위험을 감수하다
separate A from B: A를 B와 분리하다
consciousness: 의식
subjective: 주관적인
insignificant: 
disprove: 틀림을 입증하다
consistent: 
transformation: 운송수단
institution: 기관
native: 토박이의, 토종의
undergo:겪다
it is time (that) 주어 과거형 동사: 주어동사 할 시간이다
be derived from ~: ~에서 유래하다
nervous system: 신경계
all things considered: 모든것을 고려해서
against ~: 
inspiring: 영감을 주다
definitely:
audience:청중
method:방법
Part 2. 뜻 → 영단어 스펠링 쓰기 (20문항)
(철자 하나, 구두점 하나 틀리지 않도록 신중하게 작성하세요)
분해하다: break up
해가 갈수록: year by year
거의 없는: few
무려 ~ 만큼씩이나: no less than
의식 (8번 단어): consciousness
동사할 것 같다: be likely to 동사
~을 제출하다:hand in
혼자서: along
동사할 수 없다: 
극도의, 지나친: extream
주장하다: argue
느긋한: relaxed
결론을 내리다: conclude
추정하다: assume
불완전한:incomplete
불신:distrust
부족하다:lack
수리하다: repair
부자연스러운:unnatural
균열:crack
Part 3. 서술형 (공통 표현 찾기)
[주의] 세 문장에 공통으로 들어갈 표현(단어 또는 숙어 전체)을 단어장 기준으로 정확히 쓰시오.
41번
The bad weather will (                    ) our travel plans.
Your attitude can deeply (                    ) the people around you.
Smoking can negatively (                    ) your health.
(답안 작성란) : affect
42번
Some students (                    ) confidence when speaking in public.
The team lost the game because they (                    ) experience.
Plants will die if they (                    ) water and sunlight.
(답안 작성란) :
43번
The tiger managed to (                    ) from the local zoo.
He tried to (                    ) the cold reality by reading books.
The prisoners planned to (                    ) through a secret tunnel.
(답안 작성란) :
44번
The hotel will (                    ) free breakfast to all guests.
I want to (                    ) some advice to my younger brother.
The company decided to (                    ) a special discount today.
(답안 작성란) :
45번
If you practice every day, your English skills will (                    ).
The patient's condition began to (                    ) after the treatment.
We need to find ways to (                    ) our work efficiency.
(답안 작성란) :