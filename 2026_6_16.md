# 컴퓨터 구조 수행평가
## 💡 요약
CPU가 명령어를 처리하는 주소 형식(3, 2, 1, 0-주소)의 메커니즘을 이해하고, 간접 주소 지정 방식에서 레지스터 간 데이터 이동(마이크로 연산) 흐름을 마스터함.
## 어셈블리어 & 주소 지정 방식 (Addressing Mode)
CPU가 명령어를 실행할 때 오퍼랜드(데이터 또는 주소)를 어떻게 찾아가는지 결정하는 규칙.
주소표, opcode, 주소 지정방식 표가 주어진 프로그램을 보고 어셈블리어로 FETCH, DECODE, EXCUTE를 할 수 있음.


---
# 영어 단어, 시험 지문 1회 작성
## 문제 1. 배열 초기화

다음 코드의 출력 결과를 쓰시오.

```c
#include <stdio.h>

int main() {
    int arr[5] = {1, 2};

    for (int i = 0; i < 5; i++) {
        printf("%d ", arr[i]);
    }

    return 0;
}
```
12000
## 문제 2. 배열과 주소

`int`가 4바이트이고 `arr[0]`의 주소가 1000일 때, 다음 배열의 각 요소 주소를 쓰시오.
1004
1008
1012
..

## 문제 3. 문자열 크기

다음 배열의 크기는 몇 바이트인가?

```c
char str[] = "ABC";
```

--- 1바이트

## 문제 4. 포인터 기본

다음 코드의 출력 결과를 쓰시오.

```c
#include <stdio.h>

int main() {
    int a = 10;
    int *p = &a;

    *p = 30;

    printf("%d\n", a);

    return 0;
}
```
30

## 문제 5. 배열과 포인터

다음 코드의 출력 결과를 쓰시오.

```c
#include <stdio.h>

int main() {
    int arr[3] = {10, 20, 30};
    int *p = arr;

    printf("%d\n", *(p + 1));

    return 0;
}
```
20

---

## 문제 6. 2차원 배열 저장 순서

다음 배열은 메모리에 어떤 순서로 저장되는가?

```c
int arr[2][3] = {
    {1, 2, 3},
    {4, 5, 6}
};
```
0,0 0,1 0,2 1,0 1,1 1,2
---

##

## 문제 7. 포인터 배열과 배열 포인터 구분

다음 중 포인터 배열은 무엇인가?

```c
A. int *arr[5];
B. int (*arr)[5];
```
B
---

## 문제 8. 잘못된 코드 찾기

다음 코드에서 잘못된 부분을 찾고 이유를 설명하시오.

```c
int arr[3] = {1, 2, 3};
arr++;
```
배열을 늘리는건 상식적으로 안되지않나? 상수인데 
## 문제 9. 문자열 수정

다음 코드가 위험한 이유를 설명하시오.

```c
char *str = "hello";
str[0] = 'H';
```

---

## 문제 10. 동적 할당

다음 코드에서 빠진 부분을 채우시오.

```c
#include <stdio.h>
#include <stdlib.h>

int main() {
    int n = 5;
    int *arr = malloc(n * sizeof(int));

    if (arr == NULL) {
        return 1;
    }

    // 메모리 해제
    free(arr);

    return 0;
}
```